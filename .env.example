# Backend / Meta Graph (opcional)
# - Se META_SYNC_PROVIDER=meta e não houver token válido, o sync falhará.
# - Se META_SYNC_PROVIDER=stub, sempre usa provider stub (mesmo com token).
META_SYNC_PROVIDER=
META_GRAPH_VERSION=v20.0
META_ACCESS_TOKEN=

